# install.sh 
# pretty simple, so feel free to edit!
echo "welcome to my arch linux hyprland installer!"; sleep 0.7
echo "This is a rushed installer, so expect bugs, and make sure you are on arch only!"; sleep 1.3
echo "You will have 10 seconds to decide wether you can install it or not. So think! To exit, unsafe mid installation, press ctrl+C, so now sit back and relax as I install the dots for u!"; sleep 
cp -r  ~/./-ARCH-ONLY-My-Arch-Linux-Rice-Hyprland-w-/quickshell/* ~/.config/quickshell/
cp -r  ~/./-ARCH-ONLY-My-Arch-Linux-Rice-Hyprland-w-/hypr/* ~/.config/hypr/
cp -r cp -r  ~/./-ARCH-ONLY-My-Arch-Linux-Rice-Hyprland-w-/fastfetch/* ~/.config/fastfetch/
